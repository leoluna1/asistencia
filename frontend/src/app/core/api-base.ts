// ponytail: sin environment.ts con fileReplacements todavía — un solo target de build
// (dev) por ahora. Cuando exista un build de producción real, se separa.
export const API_BASE_URL = 'http://localhost:8000/api';
